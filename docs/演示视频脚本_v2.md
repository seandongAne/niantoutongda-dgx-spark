# 演示视频脚本 v2 · 把旧家的生活组合带到新家

> 目标时长：105–115 秒。
>
> 当前唯一正式入口：`results/hero/s1-auto-final-v1/index.html`。
>
> 叙事顺序：生活问题 → 可信库存 → 生活组合 → 自动空间 → 可执行任务 → 模型优化与审计。
>
> 红线：只展示已归档证据；StepFun 仅为开发期调优代理，演示运行时保持 Spark 本地、零云依赖。

## 一句话故事

搬家不只是把物品运过去，而是把熟悉的生活秩序重新拼起来。对旧家和新家各拍几段
视频，系统将嘈杂识别结果收敛为可信物品，理解哪些东西共同使用，再自动选择新家的
相对区域，生成搬家人员可以直接执行的任务卡。

## 110 秒镜头表

| # | 时间 | 画面 | 台词或字幕 | 证据 |
|---|---:|---|---|---|
| 1 | 0–8s | 旧屋杂物与新屋空景快剪；书、文具特写 | “书桌上的书和文具，搬过去后应该放在哪里？” | `local-data/hero_s1/old_{1,2,3}.mp4`、`new_1.mp4` |
| 2 | 8–20s | 三段旧房视频、旁白波形、新家巡拍并列 | 输入只有随手拍视频、一段口述旁白和新家空景。Step-Audio 2 mini 在 Spark 本地生成 30 段誊写草稿，经听校合并为 25 条结构化事实。 | `results/hero_s1/a1_transcribe/manifest.json`、`fixtures/hero_s1/transcript.txt` |
| 3 | 20–38s | 检测框、多视角物品裁剪，随后切入成果页首屏 | 3306 个原始识别结果经过核对，收敛为 20 件确认物品；跨视频完整合并达到 17/20，多策略联合 R@1 为 495/577，即 0.8579。677 条模型疑问被压成最多 4 个高价值问题，后续步骤只使用这 20 件。 | `results/hero/s1-auto-final-v1/inventory/metrics.json`、`results/acceptance/HERO_S1/multiview-closure-attempt/README.md` |
| 4 | 38–50s | 五个箱单卡片，突出学习文具与杯子饮品 | 3 个生活组合加 2 类单独收纳，形成 5 个搬运箱，完整覆盖 20/20。 | `group/metrics.json`、`group/boxlist.json` |
| 5 | 50–70s | 三张新房证据帧 → 自动空间数字条 | 新家视频找到 168 个候选区域；视觉模型自动选出 5 个互不重复的最终区域，不再需要人工填写区域编号。另一套标准独立复核，5/5 通过。 | `spatial/assignment.json`、`spatial/metrics.json`、`spatial_score/metrics.json` |
| 6 | 70–86s | 代表任务卡“学习文具箱 → 书桌”；再拉远看五卡总览 | 布局已经生成。5 张任务卡覆盖 20/20，每张卡明确箱单、目标区域、物品与验收清单。 | `layout/plan.json`、`taskcards/taskcards.md` |
| 7 | 86–100s | Nemotron 性能对比与 StepFun 调优代理各一屏 | Nemotron NVFP4 + vLLM 在同工况从 1.7 提升到 25.4 tok/s。StepFun 在开发期标注难样本并指导 Spark 本地投影头；原始澄清从 1379 降到 677，演示运行时不调用云。 | `docs/NEMOTRON_SPARK_SERVING.md`、`results/autotune/` |
| 8 | 100–110s | 完整运行记录显示流程通过；回到成果页标题 | 物品确认、生活组合、空间安排和任务卡四步全部完成，并且可以重新生成核对。收尾定格“17/20 完整合并·0.8579 多策略联合 R@1”和“把旧家的生活组合，带到新家。” | `audit/replay-report.json`、`showcase_metrics.json` |

## 录屏顺序

1. 从正式成果页首屏开始，不从终端或 JSON 开始。
2. 点击“30 秒看懂闭环”，依次录制四张故事卡。
3. 点击“新家五区”，录制三张真实证据帧和“通过 / 5/5 / 0 个需人工标注”。
4. 点击“代表任务”，只放大学习文具箱到书桌；随后用一次短镜头交代五卡覆盖 20/20。
5. NVIDIA 与 StepFun 证据各保留一屏，不滚完整日志或 sweep 表。
6. 收尾展示完整运行记录和“全流程已完成”，再回到标题页。

## 主片不展示

- 不滚动技术校验码、完整 JSON 和精确几何范围；这些收进折叠的技术复核区与答辩材料。
- 不展示旧的 `s1-final` 或 `s1-technical-closure` 页面；它们分别代表旧库存口径和人工视觉阶段。
- 不再使用“681 条消息重放”或“全包三次 hash 一致”的旧说法。当前正式主链是四条因果消息闭合，replay 为 `PASS`。
- 不说“VLM 命名权威”“完全理解家庭习惯”“最佳且唯一布局”或“保证儿童与用电安全”。
- 不再使用 AutoTune-v1 阶段的 `14/20` 和 `0.8083` 作为当前收尾数字。
  当前统一展示 `17/20` 完整合并和 `0.8579` 多策略联合 R@1。
- 不把真实摆放照片当作当前主链必需证据；物理执行复核继续作为可选延期项。

## 答辩时的边界说法

> 当前已经完成并验证的是比赛级规划闭环：可信库存、生活组合、自动空间、约束布局、
> 任务卡和审计回放。它不等于任何家庭、任何房间都能无确认完成，也不构成物理摆放
> 或安全认证。

> 身份复原的当前汇报数字是完整合并 `17/20`、多策略联合 R@1
> `495/577 = 0.8579`。后者是 baseline、v2、v3 的正确命中并集，不写成
> 某个单工作点的 R@1。
