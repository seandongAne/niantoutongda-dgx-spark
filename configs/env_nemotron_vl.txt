# Nemotron-Nano-12B-v2-VL-BF16 已验证 fallback 专用环境 — 官方模型卡要求
# transformers>4.53,<4.54 + mamba-ssm==2.2.5 + causal_conv1d。
# NVFP4-QAD 主路运行在 vllm_container,不使用本环境。
# 已知风险:mamba-ssm/causal_conv1d 在 aarch64 无预编译轮子,需要 nvcc 现场编译
# (bootstrap 先装 ninja+packaging,再 --no-build-isolation);编译失败的兜底是
# NGC PyTorch 容器,不是把它硬塞回主 venv。
transformers>4.53,<4.54
timm
accelerate
open_clip_torch
numpy
pillow
