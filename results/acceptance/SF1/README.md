# SF1-L1 · dev_a 诊断证据索引

> 这些运行使用现有 `dev_a` 标注与嵌入，只证明投影头训练、无泄漏切分、
> GB10 推理接线和判卷流程。英雄素材到达后必须用同一冻结协议重训；不得把
> 下列数字写成英雄素材最终收益。

## 运行裁决

| 运行 | 裁决 | 关键原因 |
|---|---|---|
| `dev-a-797c16a` | REJECT | 纯 128d 投影虽保持 R@1，但 R@5 `0.976190→0.880952`；旧 gate 过窄，不能采用。 |
| `dev-a-7ef07c5` | REJECT | 残差头守住 R@1/R@5，但主种子 margin `0.194555→0.075542`，严格 gate 正确拒绝。 |
| `dev-a-9fb39af` | ACCEPT_FOR_DEV_DIAGNOSTIC | 原始 DINO + `0.4×` metric head concat；主种子 R@1/R@5 不降、margin `0.194555→0.232772`。 |

## 最终 dev_a 接线

- 训练：Spark `NVIDIA GB10`，PyTorch `2.13.0+cu130`，229,760 参数；
  119 train / 42 validation / 17 identities，tracklet overlap = 0。
- 三种子：R@1 为 `0.880952 / 0.880952 / 0.857143`，均值 `0.873016`，
  std `0.011224`；主权重固定取第一颗预注册种子，不择优挑 seed。
- 投影权重：`dev-a-9fb39af/projection.npz`，SHA-256
  `c9db736623daec95075592120c399a9a163697aed1b087ffe21419a196079cfa`。
- 四点 match 校准：固定 margin=0，只比较 `0.80/0.82/0.84/0.86`；
  `0.80` 在 R@1、误合并和硬负不退化的前提下，完整合并最好。
- 完整 ReID 三重复 hash 完全一致。相对原 `reid-dev-a-v3`：
  - 17 锚点 R@1：`0.8068 → 0.8509`；
  - 完整合并：`4 → 5`；
  - 高置信误合并：`0 → 0`；
  - 硬负：`4/4 → 4/4`；
  - 自动链接：`156 → 257`；澄清：`525 → 505`。

## 未通过边界

`complete_merge` 总门仍只有 `5/17`，未达到 `15/17`。这说明 SF1-L1 的
训练与接线成立，但 dev_a 的碎轨/覆盖问题仍在；不允许把本目录标成完整 S3
验收通过。最终裁决见 `dev-a-9fb39af/final-reid-eval.json`。
