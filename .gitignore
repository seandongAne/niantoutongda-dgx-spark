logs/
models/
*.safetensors
*.gguf
*.bin
*.pt
__pycache__/
.DS_Store
*.inspect.ndjson

# Local-only raw and sensitive handoff data
local-data/

# API 凭据只存本地,永不进 git、永不上节点(deploy.sh 同步排除)
.env

# 本地 AI 编程代理项目纪律（含机器专属运行上下文）
AGENTS.md
CLAUDE.md
.venv/
COMMIT
