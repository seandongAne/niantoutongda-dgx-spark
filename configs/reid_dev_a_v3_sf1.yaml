version: "reid-dev-a-v3+sf1-c9db7366-m080"
created_at: "2026-07-16"
owner: "物品记忆 Agent"
reason: "dev_a SF1-L1 推理接线验证；concat 投影后四点校准选 m0.80（R@1 过门、误合并 0、完整合并 5）"
applies_to_dataset: "dev_a"
frozen: true
derived_from: "configs/reid_dev_a_v3.yaml"

embedding_dim: 768
top_k: 5

weights:
  instance: 0.80
  semantic: 0.15
  attribute: 0.00
  context: 0.00
  geometry: 0.05

thresholds:
  # 投影后的余弦尺度必须独立校准；0.80/0.82/0.84/0.86 四点中仅
  # 0.80 在守住 R@1、误合并和硬负的同时把完整合并由 3 提到 5。
  match: 0.80
  new: 0.70
  margin: 0.00
  min_quality: 0.28

stitch:
  enabled: true
  min_cosine: 0.85
  max_gap_ms: 0

filter:
  min_observations: 3

clarify:
  max_partners_per_tracklet: 2

projection:
  enabled: true
  artifact: "results/acceptance/SF1/dev-a-9fb39af/projection.npz"
  sha256: "c9db736623daec95075592120c399a9a163697aed1b087ffe21419a196079cfa"
